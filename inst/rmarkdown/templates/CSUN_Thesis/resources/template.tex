\documentclass[$if(stage)$$stage$,$endif$$if(fontsize)$$fontsize$,$endif$$if(lot)$lot,$endif$$if(lof)$lof,$endif$$if(lol)$lol,$endif$$if(proposal)$proposal,$endif$]{CSUNthesis}

\author{$author$}

\usepackage{graphicx}
\usepackage{listings}

\title{$title$}
\references{$references.style$}{$references.file$}
\committee
{$committee.chair$}
{$committee.second$}
{$committee.third$}

$if(proposal)$
\coordinator{$coordinator$}
$else$
\submitted{$submitted.month$}{$submitted.year$}

\abstract{$abstract$}
$endif$

$if(degree)$
\degree{$degree.degree$}{$degree.department$}
$endif$

$if(defense)$
\defense{$defense.dayname$}{$defense.date$}{$defense.time$}{$defense.location$}
$endif$

$if(copyrightyear)$
\copyrightyear{$copyrightyear$}
$endif$

$if(dedication)$
\dedication{$dedication$}
$endif$

$if(preface)$
\preface{$preface$}
$endif$

$if(acknowledgement)$
\acknowledgement{$acknowledgement$}
$endif$

$if(contact)$
\contact{$contact$}
$endif$

$if(collaboration)$
\collaboration{$collaboration$}
$endif$

\begin{document}

$body$

\end{document}
